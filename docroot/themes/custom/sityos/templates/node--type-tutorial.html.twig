{#
/**
 * @file
 * node--type-tutorial.html.twig — Tutorial content type template.
 *
 * Badge color: green ($color-success).
 */
#}

{% set classes = [
  'node',
  'node--type-tutorial',
  view_mode ? 'node--view-mode-' ~ view_mode|clean_class,
  not node.isPublished() ? 'node--unpublished',
] %}

<article{{ attributes.addClass(classes) }}>

  {% if not page %}
    {# Teaser view #}
    <h3 class="node__title">
      <a href="{{ url }}" rel="bookmark">{{ label }}</a>
    </h3>
  {% else %}
    {# Full page view #}
    <header class="node__header">
      <span class="node__type-badge node__type-badge--tutorial">
        {{ 'Tutorial'|t }}
      </span>
      <h1 class="node__title">{{ label }}</h1>
      <div class="node__meta">
        {% if author_name %}
          <span class="node__author">{{ author_name }}</span>
        {% endif %}
        <time class="node__date" datetime="{{ node.getCreatedTime()|date('c') }}">
          {{ date }}
        </time>
      </div>
    </header>
  {% endif %}

  <div class="node__content">
    {{ content }}
  </div>

</article>
